from models import train_volcano_model

if __name__ == "__main__":
    print("Training volcano model...")
    train_volcano_model()
    print("✅ Volcano model training complete. Check the 'models' directory for the generated files.")
